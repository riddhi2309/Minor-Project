version: "3.9"

services:
  ai-security:
    build:
      context: .
      dockerfile: docker/Dockerfile
    container_name: agentic-llm-security
    volumes:
      - ./src:/app/src
      - ./logs:/app/logs
    ports:
      - "8501:8501"
    restart: unless-stopped
